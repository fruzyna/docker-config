# BUSCH Config

This is the container configuration used on my personal web server. It originally ran using `docker compose`, but has been updated to use `podman-compose`. A `.env` file is used to configure directories and ports. Additional env files in `/secrets` store passwords.
